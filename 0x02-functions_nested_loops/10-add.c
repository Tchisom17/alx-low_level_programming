#include "main.h"
/**
 * add - adds two integers
 * @m - the number to add
 * @n - the number to add
 * Return: the result of the two integers
 */
int add(int m, int n)
{
int o = m + n;
return (0);
}
