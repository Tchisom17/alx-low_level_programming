#include "main.h"
/**
 * print_alphabet - Prints all the letters of the alphabets
 */
void print_alphabet(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
_putchar(alpha);
}
_putchar('\n');
}
